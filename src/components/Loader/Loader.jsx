import { ScaleLoader } from "react-spinners";

const Loader = () => {
  return (
    <ScaleLoader
      color="#d84343"
      cssOverride={{}}
      height={40}
      margin={1}
      width={5}
    />
  );
};

export default Loader;
